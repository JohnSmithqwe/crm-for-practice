# crm-for-practice

Статический фронт в папке **`crmfront`**:

- `index.html` — главная (типы проектов)
- `project.html` — карточка проекта

Запуск локально из каталога `crmfront`, например: `npx serve` — сайт доступен по корню, переход на проект: `/project.html`.
